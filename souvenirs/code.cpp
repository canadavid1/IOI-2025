#include "souvenirs.h"


void buy_souvenirs(int N, long long P0) {
    transaction(N-1);
    // std::vector<long long> P(N,-1);
    // std::vector<int> ct(N,0);
    // P[0] = P0;
    // for(int i = 1; i < N; i++) {
        
    // }
}